# npm 보안 설정

# package-lock.json 강제 사용
package-lock=true

# 설치 시 스크립트 실행 경고
ignore-scripts=false

# 정확한 버전 저장 (^ 대신 정확한 버전)
save-exact=true

# audit 자동 실행
audit=true

# 레지스트리 명시 (typosquatting 방지)
registry=https://registry.npmjs.org/
